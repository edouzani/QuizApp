<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                >

    <ImageView
        android:scaleType="fitXY"
        android:background="@drawable/main"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"/>

    <Button
        android:id="@+id/play_quiz"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_menu_buttons_width"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:layout_marginBottom="20dp"
        android:background="@drawable/btn_newgame_selector"
        android:gravity="center"
        android:text=""
        android:textColor="@android:color/white"
        android:textSize="17sp"/>

    <Button
        android:id="@+id/world_score"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_menu_buttons_width"
        android:layout_gravity="center"
        android:background="@drawable/btn_rating_selector"
        android:layout_below="@id/play_quiz"
        android:layout_marginBottom="20dp"
        android:gravity="center"/>

    <Button
        android:id="@+id/wiki"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_menu_buttons_width"
        android:layout_gravity="center"
        android:background="@drawable/btn_wiki_selector"
        android:layout_below="@id/world_score"
        android:layout_marginBottom="20dp"
        android:gravity="center"/>

    <ImageView
        android:layout_marginLeft="20dp"
        android:layout_marginTop="40dp"
        android:id="@+id/settings"
        android:visibility="visible"
        android:layout_width="35dp"
        android:background="@drawable/settings_icon"
        android:scaleType="centerCrop"
        android:layout_height="40dp"/>

    <ImageView
        android:layout_marginRight="20dp"
        android:layout_marginTop="40dp"
        android:id="@+id/about"
        android:layout_width="35dp"
        android:layout_alignParentRight="true"
        android:background="@drawable/info_icon"
        android:scaleType="centerCrop"
        android:layout_height="40dp"/>
</RelativeLayout>